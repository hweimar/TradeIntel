# This is a Databricks asset bundle definition for trade_intel.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: trade_intel
  uuid: 837e189b-15c7-4534-a9bc-a7d844f3a55f

include:
  - resources/*.yml

targets:
  prod:
    mode: production
    default: true
    workspace:
      host: https://dbc-51a2225b-a71b.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/hunterweimar@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/${bundle.name}/
    variables:
      catalog: projects
